import React from "react"
import { Layout } from "../../components/layout"

const Components = () => {
  return (
    <Layout>
      <div className="h-screen flex items-center justify-center">
        <h1>Hello from the components page!</h1>
      </div>
    </Layout>
  )
}

export default Components
